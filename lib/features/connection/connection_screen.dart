import 'package:flutter/material.dart';

/// Screen for managing SSH connections.
///
/// Displays the list of saved hosts and provides navigation
/// to add new connections or connect to existing ones.
class ConnectionScreen extends StatelessWidget {
  /// Creates a [ConnectionScreen].
  const ConnectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement
    return const Scaffold(
      body: Center(child: Text('Connections')),
    );
  }
}
